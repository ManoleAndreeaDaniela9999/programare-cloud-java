server:
  port: 8082

spring:
  application:
    name: service2

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:3000/eureka/
    register-with-eureka: true
    instance:
      hostname: localhost
      preferIpAddress: true